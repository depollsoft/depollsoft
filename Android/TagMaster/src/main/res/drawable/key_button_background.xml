<?xml version="1.0" encoding="utf-8"?>
<!-- Tonal-button background for the shared PitchPipeButton (a plain Button, so it cannot take a
     MaterialButton style): secondary-container fill, full-round corners, state-layer ripple. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <selector>
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="24dp" />
                    <solid android:color="#1F7F7F7F" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="24dp" />
                    <solid android:color="?attr/colorSecondaryContainer" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
