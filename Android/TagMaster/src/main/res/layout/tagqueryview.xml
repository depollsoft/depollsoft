<?xml version="1.0" encoding="utf-8"?>
<!--
  A list of tags, plus the two things that happen when there is no list: nothing
  matched, or the fetch failed. Both say so in place and offer the way back.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/loadingProgressBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="true" />

        <ListView
            android:id="@+id/queryResultListView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:animateLayoutChanges="true"
            android:cacheColorHint="#00000000"
            android:clipToPadding="false"
            android:divider="?attr/colorOutlineVariant"
            android:dividerHeight="1dp"
            android:paddingTop="@dimen/tm_space_s"
            android:paddingBottom="@dimen/tm_space_xl"
            android:scrollbarStyle="outsideOverlay" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/statusContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="@dimen/tm_space_xxl"
        android:paddingEnd="@dimen/tm_space_xxl">

        <TextView
            android:id="@+id/statusTextView"
            style="@style/TextAppearance.TagMaster.FieldValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/retryButton"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/tm_space_l"
            android:minHeight="@dimen/tm_touch_target"
            android:text="@string/Retry" />
    </LinearLayout>

</FrameLayout>
