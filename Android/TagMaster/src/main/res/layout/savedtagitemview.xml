<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:background="?attr/selectableItemBackground">

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/savedTagRemove"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="12dp"
        android:scaleType="center"
        android:visibility="gone"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/saved_list_remove"
        app:srcCompat="@drawable/ic_remove_circle_outline"
        app:tint="?attr/colorPrimary" />

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <depollsoft.tagmaster.BarberPoleLoadingView
            android:id="@+id/loadingBar"
            style="@style/Widget.TagMaster.CompactLoader"
            android:layout_gravity="center"
            android:contentDescription="@string/home_loading_tag" />

        <LinearLayout
            android:id="@+id/failedToLoad"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:orientation="horizontal"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/failed_to_load_tag"
                android:textAppearance="?attr/textAppearanceBodyMedium" />

            <TextView
                android:id="@+id/tagId"
                android:layout_marginStart="4dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodyMedium" />
        </LinearLayout>

        <depollsoft.tagmaster.TagItemView
            android:id="@+id/tagItemView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Just to hold vertical space while loading. -->
        <depollsoft.tagmaster.TagItemView
            android:id="@+id/dummyTagItemView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:importantForAccessibility="noHideDescendants"
            android:visibility="invisible" />
    </FrameLayout>

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/savedTagDragHandle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:padding="12dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:visibility="gone"
        android:scaleType="center"
        android:contentDescription="@string/saved_list_drag"
        app:srcCompat="@drawable/ic_drag_handle"
        app:tint="?attr/colorOnSurfaceVariant" />
</LinearLayout>
