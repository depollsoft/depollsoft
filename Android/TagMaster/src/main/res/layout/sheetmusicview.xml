<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".SheetMusicActivity">

    <include layout="@layout/app_toolbar" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">
        <com.github.chrisbanes.photoview.PhotoView
            android:id="@+id/photoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <depollsoft.tagmaster.BarberPoleLoadingView
            android:id="@+id/sheetMusicLoading"
            style="@style/Widget.TagMaster.CompactLoader"
            android:layout_gravity="center"
            android:contentDescription="@string/detail_loading" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/keyButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginBottom="16dp"
            android:layout_marginEnd="16dp"
            android:minHeight="48dp"
            android:textAllCaps="false"
            android:textColor="@color/key_button_foreground"
            app:backgroundTint="@color/sheet_key_fill"
            app:strokeColor="@color/key_button_stroke"
            app:strokeWidth="1dp"
            app:iconTint="@color/key_button_foreground"
            app:icon="@drawable/ic_key" />
    </FrameLayout>
</LinearLayout>
