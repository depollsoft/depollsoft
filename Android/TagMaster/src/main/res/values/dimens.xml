<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="widget_margin">8dp</dimen>
    <dimen name="widget_font_size">18sp</dimen>

    <!-- Reading measure for lists and forms: content is centered and capped at this width on
         tablets and in landscape (see View.applyContentInsets in EdgeToEdge.kt). -->
    <dimen name="content_max_width">640dp</dimen>
    <!-- Summary and Tracks lay out as two columns in landscape (layout-land), so they may use a
         wider measure there (values-land/dimens.xml). -->
    <dimen name="two_column_max_width">640dp</dimen>
</resources>
