apply plugin: 'com.android.library'

dependencies {
    api fileTree(include: '*.jar', dir: 'libs')
    api project(':DepollSoftCommon')
}

android {
    namespace = 'depollsoft.lib.compat'
    compileSdk 35

    defaultConfig {
        minSdk 21
    }
}
