# SDK 35 for every test class; one Android runtime per test JVM.
sdk=35

# A plain Application keeps Firebase and Facebook startup out of unit tests.
application=android.app.Application
qualifiers=w360dp-h640dp-mdpi
