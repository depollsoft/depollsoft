# Robolectric configuration
# SDK version to use for tests that do not override it.
sdk=28

# Keep unit tests isolated from Firebase/Facebook startup while allowing
# resource-backed UI tests to inflate the real application layouts.
application=android.app.Application
qualifiers=w360dp-h640dp-mdpi
