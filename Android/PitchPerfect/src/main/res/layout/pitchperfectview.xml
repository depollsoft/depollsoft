<?xml version="1.0" encoding="utf-8"?>
<!--
IMPECCABLE DIRECTION (seed 1d4ba7fb · bolder re-roll 1 · form: The Laboratory Instrument, challenger signals-instruments-nixie-laboratory-counter)
THESIS: The app is a piece of precision laboratory instrument hardware, not a grid of platform buttons; it refuses the stock utility look and all decorative color.
OWN-WORLD: Grayscale to the bone — blackened-steel panel, brushed texture, hairline anode rings, engraved condensed caps and mono digits; ONE luminous white-hot glow that exists only where sound exists. Bench-aluminum light variant.
STORY: A singer opens a lab instrument, touches a glass cell, and the note ignites — glow, name, and live frequency digits; release and the panel goes dark.
FIRST VIEWPORT: A custom-drawn radial instrument face fills the page: 12 glass cells in a true ring, frequency readout and machined range selector in the hole, nameplate caption, fixed bottom ad slot, nav bar.
FORM: Foreign form fused with product truth; won the bolder hand on both axes; grayscale pinned by user.
FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:id="@+id/mainLayout"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:orientation="vertical">

    <FrameLayout
        android:id="@+id/frameLayout1"
        android:layout_width="fill_parent"
        android:layout_height="0dip"
        android:layout_weight="1">

        <depollsoft.pitchperfect.CropFillImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            app:srcCompat="@color/plate_ground"/>

        <depollsoft.pitchperfect.HeritageScoreView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:importantForAccessibility="no" />

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <TextView
        android:id="@+id/removeAds"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:text="@string/RemoveAds"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textStyle="italic"
        android:layout_marginEnd="4dp"/>

    <FrameLayout
        android:id="@+id/adContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:itemIconTint="@color/bottom_nav_item"
        app:itemTextColor="@color/bottom_nav_item"
        app:menu="@menu/navigationmenu">

    </com.google.android.material.bottomnavigation.BottomNavigationView>
</LinearLayout>
