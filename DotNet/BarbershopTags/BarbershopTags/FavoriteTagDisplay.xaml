<UserControl x:Class="BarbershopTags.FavoriteTagDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:slabControls="clr-namespace:SLaB.Controls.Phone;assembly=SLaB.Controls.Phone"
             xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
             mc:Ignorable="d"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480"
             d:DesignWidth="480">
    <Grid x:Name="LayoutRoot">
        <slabControls:BusyIndicator x:Name="BusyIndicator">
            <toolkit:ContextMenuService.ContextMenu>
                <toolkit:ContextMenu Opened="ContextMenuOpened"
                                     IsZoomEnabled="False">
                    <toolkit:MenuItem Header="remove from favorites"
                                      Click="FavoriteRemoveClick" />
                    <toolkit:MenuItem x:Name="MoveUpItem"
                                      Header="move up"
                                      Click="MoveUpClick" />
                    <toolkit:MenuItem x:Name="MoveDownItem"
                                      Header="move down"
                                      Click="MoveDownClick" />
                </toolkit:ContextMenu>
            </toolkit:ContextMenuService.ContextMenu>
            <ContentPresenter x:Name="Presenter" />
        </slabControls:BusyIndicator>
    </Grid>
</UserControl>
