<UserControl x:Class="DePhoneTunes.Songs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:DePhoneTunes"
             mc:Ignorable="d"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480"
             d:DesignWidth="480">

    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="Add Song" Click="AddSongClick" />
        <ContentControl Grid.Column="1"
                        HorizontalContentAlignment="Stretch"
                        IsEnabled="{Binding Converter={StaticResource BoolConverter}, Path=SelectedSong}"
                        Content="{Binding}"
                        ContentTemplate="{Binding Source={StaticResource SettingsModel}, Path=UseToggleNotes, Converter={StaticResource TemplateSwitcher}}" />
        <ListBox x:Name="songListBox"
                 ItemsSource="{Binding Songs}"
                 SelectedItem="{Binding Mode=TwoWay, Path=SelectedSong}"
                 ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                 ItemTemplate="{StaticResource SongListItemDT}"
                 Grid.Row="1"
                 Grid.ColumnSpan="2">
        </ListBox>
    </Grid>
</UserControl>
