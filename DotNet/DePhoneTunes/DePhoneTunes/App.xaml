<Application x:Class="DePhoneTunes.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:slabUtils="clr-namespace:SLaB.Utilities.Phone;assembly=SLaB.Utilities.Phone"
             xmlns:converters="clr-namespace:SLaB.Utilities.Xaml.Converters;assembly=SLaB.Utilities.Xaml.Phone"
             xmlns:local="clr-namespace:DePhoneTunes"
             xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
             xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:controlsToolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
             xmlns:slabControls="clr-namespace:SLaB.Controls.Phone;assembly=SLaB.Controls.Phone">

    <!--Application Resources-->
    <Application.Resources>
        <local:AccidentalStringConverter x:Key="AccidentalConverter" />
        <local:NullVisibilityConverter x:Key="NullVisibilityConverter" />
        <local:NullVisibilityConverter x:Key="InvertedNullVisibilityConverter"
                                       Invert="True" />
        <local:MajorKeyStringConverter x:Key="MajorKeyStringConverter" />
        <local:MinorKeyStringConverter x:Key="MinorKeyStringConverter" />
        <local:KeyBoolConverter x:Key="KeyBoolConverter" />
        <local:ToLowerConverter x:Key="ToLowerConverter" />
        <local:InverterConverter x:Key="InverterConverter" />
        <converters:StringFormatConverter x:Key="StringFormat" />
        <converters:BoolConverter x:Key="BoolConverter" />

        <DataTemplate x:Key="NoteName">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FriendlyName}"
                           FontSize="26.667" />
                <TextBlock Text="{Binding Accidental, Converter={StaticResource AccidentalConverter}}"
                           FontFamily="NOTEHEDZ170.TTF#NoteHedz"
                           FontSize="40" />
                <TextBlock Text="{Binding Octave}"
                           VerticalAlignment="Bottom"
                           Margin="0,3,0,0"
                           FontSize="21.333" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="NoteDT">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource NoteName}"
                                    Content="{Binding}" />
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding Alternate, Converter={StaticResource NullVisibilityConverter}}">
                        <TextBlock Text="/"
                                   FontSize="26.667" />
                        <ContentControl ContentTemplate="{StaticResource NoteName}"
                                        Content="{Binding Alternate}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <TextBlock Text="{Binding Frequency, Converter={StaticResource StringFormat}, ConverterParameter='\{0:0.##\}'}"
                               FontSize="26.667" />
                    <TextBlock Text=" Hz"
                               FontSize="26.667" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="MajorKeyNoteName">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FriendlyName}"
                           FontSize="26.667" />
                <TextBlock Text="{Binding Accidental, Converter={StaticResource AccidentalConverter}}"
                           FontFamily="NOTEHEDZ170.TTF#NoteHedz"
                           FontSize="40" />
            </StackPanel>
        </DataTemplate>

        <Style x:Key="ListBoxItemStyle"
               TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
        </Style>

        <DataTemplate x:Key="PitchPipeNoteDT">
            <StackPanel Orientation="Horizontal">
                <ContentControl ContentTemplate="{StaticResource MajorKeyNoteName}"
                                Visibility="{Binding Alternate, Converter={StaticResource InvertedNullVisibilityConverter}}"
                                Content="{Binding}" />
                <StackPanel Orientation="Horizontal"
                            Visibility="{Binding Alternate, Converter={StaticResource NullVisibilityConverter}}">
                    <TextBlock FontFamily="NOTEHEDZ170.TTF#NoteHedz"
                               FontSize="40">
                            <TextBlock.Text>
                                <Binding Converter="{StaticResource AccidentalConverter}">
                                    <Binding.Source>
                                        <local:Accidental>Sharp</local:Accidental>
                                    </Binding.Source>
                                </Binding>
                            </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="/"
                               FontSize="26.667" />
                    <TextBlock FontFamily="NOTEHEDZ170.TTF#NoteHedz"
                               FontSize="40">
                                                        <TextBlock.Text>
                            <Binding Converter="{StaticResource AccidentalConverter}">
                                <Binding.Source>
                                    <local:Accidental>Flat</local:Accidental>
                                </Binding.Source>
                            </Binding>
                                </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="MajorKeyDT">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="MUSIQWIKB.TTF#MusiQwikB"
                               FontSize="55"
                               Margin="0,-12"
                               Text="{Binding Converter={StaticResource MajorKeyStringConverter}}" />
                </StackPanel>
                <ContentControl Grid.Column="1"
                                HorizontalAlignment="Right"
                                ContentTemplate="{StaticResource MajorKeyNoteName}"
                                Content="{Binding}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="MinorKeyNoteName">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FriendlyName, Converter={StaticResource ToLowerConverter}}"
                           FontSize="26.667" />
                <TextBlock Text="{Binding Accidental, Converter={StaticResource AccidentalConverter}}"
                           FontFamily="NOTEHEDZ170.TTF#NoteHedz"
                           FontSize="40" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="MinorKeyDT">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="MUSIQWIKB.TTF#MusiQwikB"
                               FontSize="55"
                               Margin="0,-12"
                               Text="{Binding Converter={StaticResource MinorKeyStringConverter}}" />
                </StackPanel>
                <ContentControl Grid.Column="1"
                                HorizontalAlignment="Right"
                                ContentTemplate="{StaticResource MinorKeyNoteName}"
                                Content="{Binding}" />
            </Grid>
        </DataTemplate>

        <local:KeyTypeConverter x:Key="KeyTypeConverter"
                                MajorValue="{StaticResource MajorKeyNoteName}"
                                MinorValue="{StaticResource MinorKeyNoteName}" />

        <local:KeyTypeConverter x:Key="SongKeyTypeConverter"
                                MajorValue="{StaticResource MajorKeyDT}"
                                MinorValue="{StaticResource MinorKeyDT}" />

        <DataTemplate x:Key="SongKeyDT">
            <ContentControl Content="{Binding Note}"
                            HorizontalContentAlignment="Stretch"
                            ContentTemplate="{Binding KeyType, Converter={StaticResource KeyTypeConverter}}">
            </ContentControl>
        </DataTemplate>

        <DataTemplate x:Key="KeyPickerDT">
            <ContentControl Content="{Binding Note}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            ContentTemplate="{Binding KeyType, Converter={StaticResource SongKeyTypeConverter}}" />
        </DataTemplate>

        <Style x:Key="HyperlinkButtonStyle"
               TargetType="HyperlinkButton">
            <Setter Property="Foreground"
                    Value="{StaticResource PhoneForegroundBrush}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="FontSize"
                    Value="{StaticResource PhoneFontSizeMedium}" />
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="HyperlinkButton">
                        <Border Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver" />
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Duration="0"
                                                             To="0.5"
                                                             Storyboard.TargetProperty="Opacity"
                                                             Storyboard.TargetName="TextElement" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <!--<Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                                           Storyboard.TargetName="TextElement">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneDisabledBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>-->
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter x:Name="TextElement"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ToggleTemplate">
            <ToggleButton Content="Play"
                          IsChecked="{Binding ShouldPlay, Mode=TwoWay}" />
        </DataTemplate>
        <DataTemplate x:Key="ContinuousTemplate">
            <slabControls:ContinuousButton Content="Play"
                                           IsDepressed="{Binding ShouldPlay, Mode=TwoWay}" />
        </DataTemplate>
        <converters:SwitchConverter x:Key="TemplateSwitcher">
            <converters:KeyValue Value="{StaticResource ToggleTemplate}">
                <converters:KeyValue.Key>
                    <sys:Boolean>True</sys:Boolean>
                </converters:KeyValue.Key>
            </converters:KeyValue>
            <converters:KeyValue Value="{StaticResource ContinuousTemplate}">
                <converters:KeyValue.Key>
                    <sys:Boolean>False</sys:Boolean>
                </converters:KeyValue.Key>
            </converters:KeyValue>
        </converters:SwitchConverter>

        <DataTemplate x:Key="SongListItemDT">
            <local:SongItemDisplay />
        </DataTemplate>

        <Style TargetType="controlsToolkit:ListPicker"
               x:Key="ListPickerStyle">
            <Setter Property="Background"
                    Value="{StaticResource PhoneTextBoxBrush}" />
            <Setter Property="Foreground"
                    Value="{StaticResource PhoneTextBoxForegroundBrush}" />
            <Setter Property="FontSize"
                    Value="{StaticResource PhoneFontSizeMediumLarge}" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="Margin"
                    Value="{StaticResource PhoneTouchTargetOverhang}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controlsToolkit:ListPicker">
                        <StackPanel>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="PickerStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="Expanded">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                                           Storyboard.TargetProperty="Background"
                                                                           Duration="0">
                                                <DiscreteObjectKeyFrame Value="{StaticResource PhoneTextBoxEditBackgroundColor}"
                                                                        KeyTime="0" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                                           Storyboard.TargetProperty="BorderBrush"
                                                                           Duration="0">
                                                <DiscreteObjectKeyFrame Value="{StaticResource PhoneTextBoxEditBorderBrush}"
                                                                        KeyTime="0" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentControl Content="{TemplateBinding Header}"
                                            ContentTemplate="{TemplateBinding HeaderTemplate}"
                                            Foreground="{StaticResource PhoneSubtleBrush}"
                                            FontSize="{StaticResource PhoneFontSizeNormal}"
                                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            Margin="0 0 0 8" />
                            <Grid>
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding Background}"
                                        BorderThickness="2">
                                    <Canvas x:Name="ItemsPresenterHost"
                                            MinHeight="46">
                                        <ItemsPresenter x:Name="ItemsPresenter">
                                            <ItemsPresenter.RenderTransform>
                                                <TranslateTransform x:Name="ItemsPresenterTranslateTransform" />
                                            </ItemsPresenter.RenderTransform>
                                        </ItemsPresenter>
                                    </Canvas>
                                </Border>
                                <Popup x:Name="FullModePopup">
                                    <Border Background="{StaticResource PhoneChromeBrush}">
                                        <!-- Popup.Child should always be a Border -->
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <ContentControl Grid.Row="0"
                                                            Content="{TemplateBinding FullModeHeader}"
                                                            Foreground="{StaticResource PhoneForegroundBrush}"
                                                            FontFamily="{StaticResource PhoneFontFamilySemiBold}"
                                                            FontSize="{StaticResource PhoneFontSizeMedium}"
                                                            HorizontalAlignment="Left"
                                                            Margin="24 12 0 0" />
                                            <ListBox x:Name="FullModeSelector"
                                                     Grid.Row="1"
                                                     ItemTemplate="{TemplateBinding ActualFullModeItemTemplate}"
                                                     FontSize="{TemplateBinding FontSize}"
                                                     Margin="{StaticResource PhoneMargin}"
                                                     ItemContainerStyle="{StaticResource ListBoxItemStyle}">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel />
                                                        <!-- Ensures all containers will be available during the Loaded event -->
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                            </ListBox>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </Grid>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <local:SettingsModel x:Key="SettingsModel" />
    </Application.Resources>

    <Application.ApplicationLifetimeObjects>
        <!--Required object that handles lifetime events for the application-->
        <shell:PhoneApplicationService Launching="Application_Launching"
                                       Closing="Application_Closing"
                                       Activated="Application_Activated"
                                       Deactivated="Application_Deactivated" />
        <slabUtils:XnaDispatcherService />
    </Application.ApplicationLifetimeObjects>

</Application>