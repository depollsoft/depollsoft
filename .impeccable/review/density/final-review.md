disposition: ship

Inline finish-reviewer substitution; independent read-only review of production files. No separate comp, QUALITY BAR card or seeded new-world contract supplied; this is a scoped refinement against the user request and approved `47a48b42`, not a new-world review.

## persistence

Pass for density/list separation. Verified root `/Users/depoll/.local/share/pi-worktrees/20260907005637/tm-refine-worktree-20260907` and HEAD `47a48b42`. Reviewed the current production diff, new Favorites implementations, fixture, relevant tests, ledger and both density reports. Reports were evidence indexes, not verdicts. `git diff --check` passed. Only this review file was written; no builds, tests, emulator operations, delegates or commits.

`PRODUCT.md`, both platform `DESIGN.md` records and their sidecar changes describe the two existing built-ins and future-only labels/many lists. No production model, migration, backend or sync changes appear in the diff. No custom-list editor, storage abstraction or label feature ships.

Favorites is registered in `Android/TagMaster/src/main/AndroidManifest.xml` and in the iOS project file/source build phase. Home routes directly to the new destinations. iOS fixture declarations, implementation and launch call are all guarded by `#if DEBUG`; checked project and app Release configurations do not define DEBUG. The checked Release source configuration has no synthetic launch path. This is mechanical exclusion evidence, not a Release binary inspection. Fixture/cleanup startup returns before Firebase configuration; the next ordinary Debug startup restores the backup before continuing. The saved `ios-resume-phone.log` confirms the interrupted-fixture restoration test passed.

Android historical preference restoration remains unverified. The before/after archive hashes differ, as the retained files confirm. Tar metadata and copy timestamps can explain this, but cannot prove identical preference contents. The deleted backup and missing before-file hashes prevent retrospective verification. No recovery or fabricated verification attempted. This evidence limitation is retained, not treated as a newly introduced density defect.

## fidelity

All eight mandatory captures were opened individually with unchanged Pi image-read results. Each is within 1000px on its longest edge and below 300KB. Each shows its named content without blank or broken capture regions. The iOS long-title confirmation is intentionally scrolled; the complete target title and material line are visible. The tablet Favorites image renders landscape.

| Evidence | Direct observation |
| --- | --- |
| `ios-phone-home.png` | Four discovery actions, then two peer list entries with 100/100 counts; no saved-tag feed. |
| `ios-phone-favorites.png` | Favorites title, Home Back, Edit, compact rows and alternate title. |
| `ios-phone-teachable.png` | Separate Teachable Tags title and Edit; overlapping IDs appear in this list independently. |
| `ios-phone-long-title-dark-large-confirm.png` | Long title wraps fully at accessibility size, followed by ID/material status. |
| `ios-tablet-favorites.png` | Readable-width landscape list, native Back/Edit and page-scale pole. |
| `android-phone-light-home.png` | Two peer disclosures with 24/24 counts below discovery, without duplicate feeds. |
| `android-phone-dark-large-favorites.png` | Separate Favorites page, native Up, readable large text and compact status rows. |
| `android-tablet-light-browse.png` | Live catalog with title, optional alternate and ID/material status; retained full-width Browse. |

| Scoped element | Result and evidence |
| --- | --- |
| Home structure at 0/1/100 | Match. iOS data source always returns four discovery rows and two list rows. Android binds only counts to two entries. Read test assertions cover all three counts; Android also asserts unchanged measured height. |
| Separate lists and membership | Match. Each destination reads its own existing ordered membership. iOS move/remove methods and Android context-menu handlers target only their respective model. Tests assert overlap, independent order and removals in both directions. |
| Edit and native Back | Match. iOS Favorites owns Edit/Done with updated accessibility labels, removal and reordering. Android reuses existing long-press move/remove controls and the native Back dispatcher. Saved native logs support navigation/mutation checks. |
| Compact rows and metadata | Match. Shared catalog/saved rows prioritize complete title, distinct alternate, ID and read-only material availability. Summary/Details files are unchanged; rating and Android posted/download fields remain registered there. No metadata model fields were removed. |
| TYPE | Match. Handwritten identity remains; native semantic UI type, unlimited title lines and automatic/wrap-content heights preserve accessibility scaling. iOS rows have a 60pt floor; Android entries/rows retain the 48dp minimum. |
| MATERIAL | Match. Original full barber-pole artwork remains the page background, not a footer substitute or opaque-card decoration. |
| GROUND | Match within preservation scope. Background/theme artwork is unchanged from the approved correction; captured light/dark grounds retain the pole. No new palette or ground treatment is introduced. |
| Empty/refreshed lists | Match. iOS empty states are children of the retained table background, toggled without replacing it. Notifications reload rows while retaining the background. Android installs PoleBackground on both saved-list roots independently of empty/loading content. |
| Tablet adaptation | Acceptable adaptation. iOS and Android saved destinations retain readable-width behavior. Android Browse keeps its incumbent full-width layout; the user requested density/list separation, not a catalog-width redesign. |
| Future organization | Match. Repeated disclosures can accommodate more entries later; only Favorites and Teachable Tags exist now. |

## ceiling

Scoped requirements covered. No material density/list-separation gap found in the inspected code and eight captures. Do not add ornament, a theme, tabs or new list infrastructure to raise an unrelated design score.

Evidence ceiling: all iOS saved images and Android saved-list images use explicitly documented local synthetic data. Android Browse is live catalog evidence. Android tablet coverage is an 800dp runtime simulation, not tablet hardware. Saved iOS resume logs confirm six passing executions across phone, large text and iPad; Android native logs confirm 4 + 1 + 1 executions. These are incremental saved results, not a freshly green full suite.

No hardware, VoiceOver/TalkBack listening, measured contrast audit, Release build or exhaustive orientation/text-size matrix was performed. iOS reorder has controller-method and native editing-control evidence, not a drag gesture. Android Teachable scroll restoration lacks its own native assertion. Those limits, and the preference-restoration gap above, remain explicit. This verdict does not certify the whole app for release.

## material_fixes

None within density/list separation.

## keep

Preserve `47a48b42` character: full barber pole, direct native push/Back, two equal Home list entries, independent memberships and complete metadata on tag pages; leave custom labels, many-list management, storage/migrations/sync, footer polish and unrelated media work outside this change.
