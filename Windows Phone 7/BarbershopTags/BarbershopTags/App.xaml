<Application x:Class="BarbershopTags.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
             xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
             xmlns:slab="clr-namespace:SLaB.Utilities.Phone;assembly=SLaB.Utilities.Phone"
             xmlns:tilt="clr-namespace:Microsoft.Unsupported;assembly=SLaB.Controls.Phone"
             xmlns:local="clr-namespace:BarbershopTags"
             xmlns:converters="clr-namespace:SLaB.Utilities.Xaml.Converters;assembly=SLaB.Utilities.Xaml.Phone"
             xmlns:my="clr-namespace:SLaB.Controls.Phone;assembly=SLaB.Controls.Phone">

    <!--Application Resources-->
    <Application.Resources>
        <Style TargetType="RadioButton"
               x:Key="ToggleRadioButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <ToggleButton Content="{TemplateBinding Content}"
                                      FontSize="{StaticResource PhoneFontSizeSmall}"
                                      IsChecked="{Binding IsChecked, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ImageBrush x:Key="ImageBackgroundBrush"
                    AlignmentY="Top"
                    ImageSource="/BarbershopTags;component/ScreenBackground.png"
                    Opacity=".3"
                    Stretch="None" />
        <Style x:Key="PhoneButtonBase"
               TargetType="ButtonBase">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource PhoneForegroundBrush}" />
            <Setter Property="Foreground"
                    Value="{StaticResource PhoneForegroundBrush}" />
            <Setter Property="BorderThickness"
                    Value="{StaticResource PhoneBorderThickness}" />
            <Setter Property="FontFamily"
                    Value="{StaticResource PhoneFontFamilySemiBold}" />
            <Setter Property="FontSize"
                    Value="{StaticResource PhoneFontSizeMediumLarge}" />
            <Setter Property="Padding"
                    Value="10,3,10,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ButtonBase">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver" />
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                                           Storyboard.TargetName="ContentContainer">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneBackgroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                                           Storyboard.TargetName="ButtonBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneForegroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush"
                                                                           Storyboard.TargetName="ButtonBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneForegroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                                           Storyboard.TargetName="ContentContainer">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneDisabledBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush"
                                                                           Storyboard.TargetName="ButtonBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneDisabledBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                                           Storyboard.TargetName="ButtonBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="Transparent" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="ButtonBackground"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="0"
                                    Margin="{StaticResource PhoneTouchTargetOverhang}">
                                <ContentControl x:Name="ContentContainer"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Content="{TemplateBinding Content}"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                Padding="{TemplateBinding Padding}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PhoneRadioButtonCheckBoxBase"
               BasedOn="{StaticResource PhoneButtonBase}"
               TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="{StaticResource PhoneRadioCheckBoxBrush}" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource PhoneRadioCheckBoxBrush}" />
            <Setter Property="FontSize"
                    Value="{StaticResource PhoneFontSizeMedium}" />
            <Setter Property="FontFamily"
                    Value="{StaticResource PhoneFontFamilyNormal}" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="Padding"
                    Value="0" />
        </Style>
        <Style x:Key="SmallCheckBoxStyle"
               BasedOn="{StaticResource PhoneRadioButtonCheckBoxBase}"
               TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver" />
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                                           Storyboard.TargetName="CheckBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneRadioCheckBoxPressedBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush"
                                                                           Storyboard.TargetName="CheckBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneRadioCheckBoxPressedBorderBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill"
                                                                           Storyboard.TargetName="CheckMark">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneRadioCheckBoxCheckBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill"
                                                                           Storyboard.TargetName="IndeterminateMark">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneRadioCheckBoxCheckBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <!--<Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="CheckBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneRadioCheckBoxDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush" Storyboard.TargetName="CheckBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill" Storyboard.TargetName="CheckMark">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneRadioCheckBoxCheckDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill" Storyboard.TargetName="IndeterminateMark">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneRadioCheckBoxCheckDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="ContentContainer">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>-->
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility"
                                                                           Storyboard.TargetName="CheckMark">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <Visibility>Visible</Visibility>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility"
                                                                           Storyboard.TargetName="XMark">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <Visibility>Collapsed</Visibility>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked" />
                                    <VisualState x:Name="Indeterminate">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility"
                                                                           Storyboard.TargetName="IndeterminateMark">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <Visibility>Visible</Visibility>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="CheckBackground"
                                        Visibility="Collapsed"
                                        BorderBrush="{TemplateBinding Background}"
                                        BorderThickness="{StaticResource PhoneBorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        HorizontalAlignment="Left"
                                        Height="32"
                                        IsHitTestVisible="False"
                                        VerticalAlignment="Center"
                                        Width="32" />
                                <Rectangle x:Name="IndeterminateMark"
                                           Fill="{StaticResource PhoneRadioCheckBoxCheckBrush}"
                                           HorizontalAlignment="Center"
                                           Height="16"
                                           IsHitTestVisible="False"
                                           Grid.Row="0"
                                           Visibility="Collapsed"
                                           VerticalAlignment="Center"
                                           Width="16" />
                                <Path x:Name="XMark"
                                      Data="M0,0 L31,31 M0,31 L31,0 z"
                                      Fill="{StaticResource PhoneAccentBrush}"
                                      Stroke="{StaticResource PhoneAccentBrush}"
                                      HorizontalAlignment="Center"
                                      Height="20"
                                      IsHitTestVisible="False"
                                      Stretch="Fill"
                                      StrokeThickness="4"
                                      StrokeLineJoin="Bevel"
                                      Visibility="Visible"
                                      VerticalAlignment="Center"
                                      Width="20" />
                                <Path x:Name="CheckMark"
                                      Data="M0,119 L31,92 L119,185 L267,0 L300,24 L122,250 z"
                                      Fill="{StaticResource PhoneAccentBrush}"
                                      HorizontalAlignment="Center"
                                      Height="18"
                                      IsHitTestVisible="False"
                                      Stretch="Fill"
                                      StrokeThickness="2"
                                      StrokeLineJoin="Round"
                                      Visibility="Collapsed"
                                      VerticalAlignment="Center"
                                      Width="24" />
                                <ContentControl x:Name="ContentContainer"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Content="{TemplateBinding Content}"
                                                Grid.Column="1"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                Margin="12,0,0,0"
                                                Padding="{TemplateBinding Padding}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HyperlinkButtonStyle"
               TargetType="HyperlinkButton">
            <Setter Property="Foreground"
                    Value="{StaticResource PhoneForegroundBrush}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="FontSize"
                    Value="{StaticResource PhoneFontSizeMedium}" />
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="HyperlinkButton">
                        <Border Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver" />
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Duration="0"
                                                             To="0.5"
                                                             Storyboard.TargetProperty="Opacity"
                                                             Storyboard.TargetName="TextElement" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <!--<Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                                           Storyboard.TargetName="TextElement">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{StaticResource PhoneDisabledBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>-->
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border Background="{TemplateBinding Background}"
                                    Margin="{StaticResource PhoneHorizontalMargin}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter x:Name="TextElement"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <converters:StringFormatConverter x:Key="StringFormat" />
        <converters:BoolConverter x:Key="BoolConverter" />
        <converters:CollectionConverter x:Key="CollectionConverter" />
        <local:EscapeQueryParamConverter x:Key="EscapeQueryParam" />
        <local:RangeCollectionConverter x:Key="RangeCollectionConverter" />
        <DataTemplate x:Key="TagLink">
            <HyperlinkButton Margin="0,10,0,10"
                             HorizontalAlignment="Left"
                             HorizontalContentAlignment="Left"
                             NavigateUri="{Binding Id, Converter={StaticResource StringFormat}, ConverterParameter='/TagPresenterPage.xaml?id=\{0\}'}"
                             Style="{StaticResource HyperlinkButtonStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding Title}"
                               Foreground="{StaticResource PhoneAccentBrush}"
                               FontSize="{StaticResource PhoneFontSizeMedium}" />
                    <TextBlock Text="{Binding AlternativeTitle, Converter={StaticResource StringFormat}, ConverterParameter='a.k.a. \{0\}'}"
                               Visibility="{Binding AlternativeTitle, Converter={StaticResource BoolConverter}}"
                               FontSize="{StaticResource PhoneFontSizeSmall}"
                               Margin="20,0,0,0" />
                    <StackPanel Orientation="Horizontal"
                                Margin="20,0,0,0">
                        <TextBlock Text="{Binding Rating, Converter={StaticResource StringFormat}, ConverterParameter='Rating: \{0:0.00\}'}"
                                   FontSize="{StaticResource PhoneFontSizeSmall}" />
                        <TextBlock Text="{Binding Posted, Converter={StaticResource StringFormat}, ConverterParameter='Posted: \{0:d\}'}"
                                   FontSize="{StaticResource PhoneFontSizeSmall}"
                                   Margin="15,0" />
                        <TextBlock Text="{Binding DownloadCount, Converter={StaticResource StringFormat}, ConverterParameter='DLs: \{0\}'}"
                                   FontSize="{StaticResource PhoneFontSizeSmall}"
                                   Margin="10,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Margin="20,0,0,0">
                        <CheckBox Content="Sheet music"
                                  IsEnabled="False"
                                  IsChecked="{Binding SheetMusicUri, Converter={StaticResource BoolConverter}}"
                                  FontSize="{StaticResource PhoneFontSizeSmall}"
                                  Style="{StaticResource SmallCheckBoxStyle}" />
                        <CheckBox Content="Learning tracks"
                                  IsEnabled="False"
                                  IsChecked="{Binding Tracks, Converter={StaticResource CollectionConverter}}"
                                  FontSize="{StaticResource PhoneFontSizeSmall}"
                                  Style="{StaticResource SmallCheckBoxStyle}"
                                  Margin="8,0" />
                    </StackPanel>
                </StackPanel>
            </HyperlinkButton>
        </DataTemplate>
        <DataTemplate x:Key="CenteredWrappingBusyContentTemplate">
            <TextBlock HorizontalAlignment="Center"
                       Text="{Binding}"
                       TextWrapping="Wrap" />
        </DataTemplate>
        <local:TeachableTagsModel x:Key="TeachableTagsModel" />
        <Style x:Key="TiltItemContainerStyle"
               TargetType="ListBoxItem">
            <Setter Property="tilt:TiltEffect.IsTiltEnabled"
                    Value="True" />
            <Setter Property="FrameworkElement.HorizontalAlignment"
                    Value="Stretch" />
            <Setter Property="Control.HorizontalContentAlignment"
                    Value="Stretch" />
        </Style>
        <DataTemplate x:Key="RatingItemTemplate">
            <Border BorderBrush="{StaticResource PhoneContrastForegroundBrush}"
                    BorderThickness="3"
                    Margin="7">
                <ItemsControl ItemsSource="{Binding Converter={StaticResource RangeCollectionConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="6">
                                <Path Data="M 0,0 l 10,0 l 5,-10 l 5,10 l 10,0 l -7,10 l 2,10 l -10,-5 l -10,5 l 2,-10 Z"
                                      Fill="{StaticResource PhoneAccentBrush}"
                                      Margin="0,7,0,0" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="VideoPreviewTemplate">
            <local:VideoPreview tilt:TiltEffect.IsTiltEnabled="True"
                                Video="{Binding}" />
        </DataTemplate>
        <DataTemplate x:Key="TrackTitleItemTemplate">
            <TextBlock FontSize="{StaticResource PhoneFontSizeLarge}"
                       Text="{Binding Title}" />
        </DataTemplate>
        <DataTemplate x:Key="VideoPresentationTemplate">
            <my:TrialBlocker>
                <ScrollViewer Grid.Row="2">
                    <StackPanel>
                        <TextBlock Style="{StaticResource PhoneTextGroupHeaderStyle}"
                                   Text="teaching video"
                                   Visibility="{Binding TeachingVideo, Converter={StaticResource BoolConverter}}" />
                        <local:TeachingVideoPreview TargetTag="{Binding}"
                                                    Visibility="{Binding TeachingVideo, Converter={StaticResource BoolConverter}}" />
                        <TextBlock Style="{StaticResource PhoneTextGroupHeaderStyle}"
                                   Text="user submissions" />
                        <TextBlock Text="Sorry, this tag does not have any videos associated with it."
                                   TextWrapping="Wrap"
                                   Visibility="{Binding Videos, Converter={StaticResource CollectionConverter}, ConverterParameter=!}" />
                        <ItemsControl ItemTemplate="{StaticResource VideoPreviewTemplate}"
                                      ItemsSource="{Binding Videos}" />
                    </StackPanel>
                </ScrollViewer>
            </my:TrialBlocker>
        </DataTemplate>
        <DataTemplate x:Key="FavoriteItemTemplate">
            <local:FavoriteTagDisplay TagId="{Binding}" />
        </DataTemplate>
        <DataTemplate x:Key="TeachableItemTemplate">
            <local:TeachableTagDisplay TagId="{Binding}" />
        </DataTemplate>
    </Application.Resources>

    <Application.ApplicationLifetimeObjects>
        <!--Required object that handles lifetime events for the application-->
        <shell:PhoneApplicationService Launching="Application_Launching"
                                       Closing="Application_Closing"
                                       Activated="Application_Activated"
                                       Deactivated="Application_Deactivated" />
        <slab:XnaDispatcherService />
    </Application.ApplicationLifetimeObjects>

</Application>