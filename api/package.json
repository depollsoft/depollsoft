{
  "name": "depollsoft-api",
  "version": "1.0.0",
  "description": "",
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "buildtest": "tsc",
    "docker-build": "docker build --tag depollsoft-api .",
    "docker-run": "docker run -it --init --env PORT=$PORT --env-file .env -p $PORT:$PORT depollsoft-api",
    "clean": "rm -rf bin",
    "test": "tsc && node --test \"bin/test/**/*.test.js\"",
    "start": "PORT=8080 npm run docker-run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/geoip-lite": "^1.4.4",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^9.0.3",
    "@google-cloud/pubsub": "^6.0.1",
    "express": "^5.2.1",
    "geoip-lite": "^2.0.3",
    "google-auth-library": "^11.0.2"
  }
}
